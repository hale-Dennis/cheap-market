{
  "name": "cheap-market-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/*"
  ],
  "scripts": {
    "install": "npm install --workspaces"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@types/jest": "^30.0.0"
  }
}
